enum routes {
  LOADING = 'loading',
  INTRO = 'intro',
  HOME = 'home',
  RATING = 'rating',
  PROFILE = 'profile',
  ADMIN = 'admin'
}
enum upload {
  INPUT,
  BUTTONS,
  LOADING,
  FINISH,
  ERROR,
  AD
}
export {
  routes,
  upload
}